export enum EFormType {
  row = 'row',
  collumn = 'collumn',
}

export enum EButtonType {
  primary = 'primary',
  alert = 'alert',
}

export enum EFormAlign {
  left = 'left',
  right = 'right',
  center = 'center',
}
